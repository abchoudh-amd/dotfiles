# Real env-var secrets live in the untracked ~/dotfiles/secrets.env (gitignored).
# install.sh seeds it from your current environment, or copy this template:
#   cp secrets.env.example secrets.env  && edit
# It is symlinked to ~/.config/secrets.env and sourced by ~/.bashrc.

export LLM_GATEWAY_KEY="REPLACE_ME"
