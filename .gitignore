# Secrets — never commit
secrets.env
secrets/

# Local overrides
*.local
**/.claude/settings.local.json

# tmux plugins are installed by TPM (prefix + I), not vendored
**/.tmux/plugins/

# lazy.nvim re-pins this on every plugin update; it is machine-local
config/nvim/lazy-lock.json

# State / logs / backups
/.claude.json
*.log
*.bak
*.swp
.DS_Store
